SPECIFICATION Spec
CONSTANT defaultInitValue = defaultInitValue
\* Add statements after this line.
CONSTANTS
    NoTask = NoTask
    NoLock = NoLock
    NoEvent = NoEvent
    NoNode = NoNode

INVARIANTS
    FinalizedGraphWellFormed
    NodesNeverExecute
    ResolutionQueueEmpty


\* Expected terminal state: graph built, main task done, nothing else to do
CHECK_DEADLOCK FALSE
